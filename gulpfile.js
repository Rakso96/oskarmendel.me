var gulp = require('gulp');


/*
  The default task is the task that runs if we just type "gulp"
*/
guld.task('default', function(){
  //Code that will run when we call the default tast
  console.log('Hello World');
});